# J-LIVE 일일 콘서트 서치 정책

Orca 자동화("J LIVE 일일서치" 워크스페이스, 매일 오후 9시 KST, Codex)가 매 실행마다 따르는 전체 정책이다. 이 파일이 단일 진실 소스이며, 정책을 바꿀 때는 이 파일만 수정하면 된다.

## 0. 범위와 목표

`j-live-cloudflare-deploy` 저장소 안에서만 작업한다. 한국에 새로 발표되는 J-POP·일본 아티스트 콘서트를 찾아 `calendar/data/events.json`을 갱신한다. 기존 스키마와 UTF-8 인코딩을 유지하고, 사용자의 관련 없는 로컬 변경사항을 절대 덮어쓰지 않는다.

**이 자동화는 미래 공연만 다룬다.** 실행 시점(KST) 기준으로 concertDate가 오늘 이후인 공연만 조사·추가·갱신 대상이다. 이미 종료됐거나 concertDate가 과거인 공연은 발견하더라도 `events.json`에 추가하지 않고, 과거 이력 파일(`historical-events-YYYY.json`) 작업도 하지 않는다. 과거 공연을 발견하면 보고서에 "과거 공연 — 미처리"로만 짧게 언급한다.

## 1. 실행 시작 절차

- 매 실행 시작 시 현재 KST 날짜를 기준으로, 새로 발표된 공연과 기존 미래 레코드를 함께 검토한다.
- 우선순위: ① 새로 발표된 일반예매/선예매 및 30일 이내 예매 오픈 건 → ② 공식 소스가 바뀐 건 → ③ 7일 이상 재검증되지 않은 건 → ④ 그 외 미래 공연.
- 값을 바꾸기 전에는 반드시 공식 소스를 다시 연다.
- `memory.md`를 런 간 체크포인트로 사용한다. 7일 이내 검증된 레코드는 판매가 임박했거나 공식 소스가 바뀐 경우가 아니면 건너뛴다.
- 새로 조사하는 후보는 런당 최대 5건으로 제한한다.
- `verifiedAt`은 실제로 공식 소스를 다시 열어 사실을 확인했을 때만 갱신한다.
- **검색 예산 관리**: 검색 도구 호출 횟수는 턴당 제한이 있다. 재검증 대상과 신규 후보 검색에 예산을 배분할 때 우선순위가 높은 항목부터 순서대로 검색한다. 검색 호출이 소진되거나 실패하면, 처리하지 못한 항목을 명확히 나열해 다음 실행의 최우선순위로 넘기고, 원인 불명의 실패를 "검색 한도"로 뭉뚱그려 보고하지 말고 실패한 정확한 시점과 시도한 쿼리를 기록한다.

## 2. 소스 우선순위와 접근 규칙

- 1순위: YES24 티켓, NOL 티켓, 멜론티켓, 티켓링크의 공식 예매 공지.
- 2순위: 공식 아티스트·기획사 웹사이트, 투어 공지, 공식 인스타그램.
- 나무위키, 블로그, 커뮤니티, 검색 요약은 **발견용(discovery-only)**으로만 쓴다. 반드시 실제 공식 페이지를 열어 확인하며, 상품 ID·스니펫·HTTP 200 응답·도메인명만으로 사실을 추정하지 않는다.
- **나무위키 활용 규칙**: 나무위키의 공연 표와 후보 사실은 발견 색인 및 각주(출처 지도)로만 사용한다. 각주나 링크를 따라간 원본 페이지가 허용된 공식 티켓/기획사/아티스트/공연장/공식 SNS 소스이고 해당 사실을 직접 검증한다면 그 사실을 사용하고, `sources`에는 원본 페이지 URL만 저장한다. 나무위키 URL 자체는 절대 출처로 저장·보고하지 않는다. 각주만으로는 검증되지 않은 것으로 간주한다 — 반드시 인용된 페이지를 열어 정확한 값을 뒷받침하는지 확인하고, 접근 불가·비공식·죽은 링크·상충·불완전이면 `pending` 처리한다. 나무위키가 링크한 비공식 기사는 해당 필드에 대해 정책상 명시적으로 허용된 경우가 아니면 발견용으로만 남긴다.
- 각 벤더·소스의 이용약관, robots 지침, 접근 제어, 합리적 요청 빈도를 지킨다. 공개적으로 접근 가능한 페이지와 정상적인 지원 내비게이션만 사용한다. 확인은 저빈도·순차적으로 하고, 이미 연 페이지를 재사용한다.
- 로그인 요구, CAPTCHA, 봇 탐지, 대기열, 요청 제한, 지역 제한, 페이월, 기술적 접근 제어를 절대 우회하지 않는다. 비공개/미문서 API 호출, 인증 토큰·쿠키 열람 또는 재사용, 모바일 앱 위장, 아이덴티티·IP 로테이션, 사이트가 금지하는 헤드리스 자동화를 하지 않는다. 좌석 예약, 대기열 진입, 장바구니 담기, 구매, 회원 전용 선예매 상호작용을 자동화하지 않는다. 개인정보를 수집하지 않는다. 벤더가 자동 접근을 차단하거나 약관상 필요한 확인이 불가능하면 해당 소스 확인을 중단하고 제한 사항을 보고한 뒤, 기획사·아티스트·공연장·벤더의 다른 공식 공개 공지로 검증한다. 필드를 채우기 위해 이 규칙들을 절대 완화하지 않는다.

## 3. 미래 이벤트 필드 검증

각 미래 이벤트에 대해 다음을 명시적으로 조사·검증한다: title, artist, concertDate, time, venue, vendor, vendorUrl, price, priceCurrency, ticketDate, ticketTime, presaleDate, presaleTime, 선예매 자격.

- 공식 소스가 상충하면 최신 공식 티켓 페이지를 우선한다.
- `confirmed` 레코드는 artist, concertDate, time, venue, vendor, vendorUrl, price, priceCurrency, ticketDate, ticketTime, 공식 유튜브 채널 핸들, 대표곡 정확히 3곡, sources, verifiedAt이 모두 구체적인 공식 값으로 채워져야 한다.
- 공식적으로 선예매가 발표되지 않은 경우에만 presaleDate/presaleTime을 비워 둔다.
- 필수 사실이 없거나 상충하거나 검증 불가능하면 상태를 `pending`으로 하고, 날짜/시간 필드는 문자열 `"pending"`을 쓰지 말고 비워 둔다. 미해결 사실과 소스 상충을 보고한다.
- **판매 상태 확인 범위**: 실시간 잔여석 수나 매진 여부를 좌석 선택 화면에 들어가서 확인하는 것은 요구하지 않는다(정책상 좌석 선택·대기열 진입 자체가 금지되어 있다). `ticketDate`/`ticketTime`이 지났고 공식 공지의 다른 모든 필수 사실이 채워졌다면, 실시간 재고를 확인하지 못했다는 이유만으로 `confirmed`를 보류하지 않는다. 공식 소스가 명시적으로 매진을 발표한 경우에만 `ticketAvailability: "sold_out"`을 기록한다.

## 4. 좌석 등급 가격 (seatPrices)

신규 추가되거나 공식적으로 변경된 모든 미래 이벤트는 공식 티켓 공지/상품 페이지에서 좌석 등급 가격을 명시적으로 조사한다. 이는 승인된 스키마 확장이다.

```json
"seatPrices": [{ "name": "공식 좌석/구역 명칭", "price": 138000, "priceCurrency": "KRW" }]
```

- 스탠딩/지정석, VIP/R/S, 플로어, 시야제한석, 패키지 혜택, 1+1 등 공식적으로 명시된 구분을 정확한 공식 한국어 명칭 그대로 사용한다.
- 가격은 쉼표나 원화 기호 없이 숫자 액면가만 저장한다. 공식 노출 순서를 유지하고, 조건·가격이 다른 등급을 합치지 않는다.
- 단일가 이벤트는 `전석` 등 정확한 공식 명칭으로 한 등급만 저장한다.
- 예매 수수료, 배송비, 할인, 쿠폰, 리셀가, 숙박 패키지, 추정값은 공식 공지가 해당 티켓 등급의 일부로 명시하지 않는 한 seatPrices에 포함하지 않는다.
- 등급이 여러 개면 기존 scalar `price`는 일반적으로 구매 가능한 액면가 중 최저가로 설정하고 `priceCurrency`는 KRW로 유지한다. 매진/초청전용/접근성전용/애드온/패키지전용 등급을 scalar 최저값으로 쓰지 않는다. 등급이 하나면 그 값이 `price`와 일치해야 한다.
- 공식 소스가 좌석별 가격을 공개하지 않으면 수용 인원, 좌석도, 스니펫, 블로그, 애그리게이터, 나무위키, 다른 투어 도시에서 추정하지 않는다. 기존 레코드는 seatPrices를 비워 두고, 신규 조사 중인 `pending` 레코드는 빈 배열을 쓰며, 누락된 가격 등급을 보고한다. 필수 가격 사실이 검증되지 않은 신규 이벤트는 `confirmed`로 확정하지 않는다.
- 가격 검증은 실제 페이지의 보이는 텍스트나 공식 공개 가격표 이미지를 사용할 수 있다. 이미지를 읽는다면 저장 전 모든 라벨과 금액을 수동으로 교차 확인한다. 판매 대기열 진입, 좌석 선택, 장바구니 담기, 로그인, 비공개 API 호출, 접근 제어 우회로 가격을 얻지 않는다. 벤더가 접근을 막으면 다른 공식 공개 아티스트/기획사/공연장 공지를 사용하고 제한 사항을 보고한다.
- 이벤트가 추가되거나 공식 가격 공지가 바뀔 때마다 저장된 모든 등급을 비교해 seatPrices, price, priceCurrency, sources, verifiedAt을 함께 갱신한다. 새로 열린 시야제한석/추가석 등급은 기존 유효 등급을 지우지 않고 반영한다. 등급명 중복을 피한다. 한국어 보고에는 변경된 각 이벤트의 좌석 등급과 가격을 나열하고 정확한 공식 소스를 링크한다.

## 5. 유튜브 대표곡

공식 아티스트·레이블 채널의 `youtube.com/watch` 직링크로 서로 다른 대표곡 정확히 3곡을 사용한다. 팬 업로드, 재업로드, 검색 페이지, 재생목록, 쇼츠, 라이브 클립, 비공식 가사 영상은 쓰지 않는다. 공개 조회수를 확인할 수 있으면 조건을 만족하는 공식 업로드 중 조회수 상위 3곡을 우선한다. 같은 아티스트의 여러 날짜는 동일하게 검증된 채널과 곡을 재사용한다.

## 6. artistAliases와 사이트 파일

새 아티스트가 추가될 때마다 `calendar/app.js`의 `artistAliases` 맵도 갱신한다. `event.artist` 값을 그대로 키로 쓰고, 흔히 쓰는 한국어 표기와 (있다면) 공식 일본어 표기를 추가한다. 기계 음역만으로 별칭을 지어내지 말고 공식 한국 예매/기획사 공지와 아티스트의 공식 일본어 페이지에서 철자를 검증한다. 기존 별칭 항목을 재사용하고 중복 생성하지 않는다. 정식 아티스트명, 한국어 별칭, 일본어 별칭으로 검색했을 때 모두 새 이벤트와 매칭되는지 확인한다. `calendar/app.js`가 바뀌면 `calendar/index.html`의 캐시 버스팅 쿼리를 올리고 `calendar/service-worker.js`의 캐시 버전을 증가시킨다.

아티스트+concertDate+venue 중복 레코드를 피한다. 공식 정보가 바뀌면 기존 레코드를 갱신한다. 취소, 연기, 추가 공연, 예매 오픈 변경, 선예매 자격 변경을 반영한다. 실제로 공식 소스를 다시 열어 검증하지 않고서는 verifiedAt을 갱신하지 않는다.

새로 확정된 아티스트나 유튜브 채널/아바타가 바뀔 때마다 `calendar/site.js`에 이미 구현된 파일명 정규화를 사용해 공식 유튜브 채널 프로필 이미지를 `calendar/assets/artists/` 아래에 캐싱한다. 뮤직비디오 썸네일을 프로필 이미지로 쓰지 않고, 임의의 원격 이미지 URL을 이벤트 데이터에 저장하지 않는다. 공식 채널 아바타를 가져와 이미지로 검증할 수 없으면 대체 이미지를 쓰지 말고 보고한다.

## 7. 과거 공연 이력 (일요일 전용 스윕)

**비활성화됨.** 이 자동화는 더 이상 과거 공연 이력을 수집하지 않는다. 일요일을 포함해 언제든 미래 공연 스윕만 수행하고, `historical-events-YYYY.json` 파일은 건드리지 않는다. (과거 정책 내용은 참고용으로 아래에 남겨두되 실행하지 않는다.)

~~KST 기준 일요일에만, 미래 공연 스윕이 끝난 뒤 2020-01-01 이후 대한민국 과거 공연 후보를 최신 연도부터 최대 5건 조사해 historical-events-YYYY.json에 저장하던 절차.~~ (중단)

## 8. 데이터 갱신 범위

현재/미래 확정 변경사항만 `calendar/data/events.json`에 반영한다. `calendar/data/historical-events-YYYY.json`은 이 자동화에서 다루지 않는다.

## 9. 검증과 배포

- 이벤트, 별칭, 캐시된 사진이 바뀌면 `node tools/generate-seo-pages.js`, `node --test tools/*.test.js`, `node --check calendar/app.js`를 실행한다. `calendar/data/events.json`이 파싱되는지, 생성된 이벤트 페이지가 변경 레코드와 일치하는지, `sitemap.xml`이 의도된 색인 가능 클린 URL만 포함하는지, `robots.txt`가 sitemap을 가리키는지 확인한다.
- `events.json`이 바뀌었으면 정책상 필요한 생성기를 실행하고, `node --test tools/*.test.js`와 `git diff --check`를 항상 실행한다. 데이터가 바뀌지 않았으면 공식으로 확인한 페이지만 보고한다.
- 모든 검사를 통과한 뒤에만 배포한다. `git status`와 `git diff`를 확인해 자동화 소유 변경사항만 스테이징하고, 날짜 기반의 간결한 메시지로 커밋한 뒤 현재 `main`을 `origin main`에 푸시한다. 강제 푸시, reset, rebase를 하지 않고, 관련 없는 사용자 변경사항을 포함하지 않는다. 워크트리에 안전한 커밋을 막는 관련 없는 변경사항이 있으면 배포하지 말고 차단 사유를 보고한다.
- 관련 없는 변경사항을 덮어쓰거나 섞인 변경사항을 배포하지 않는다. 자동화 소유 변경사항만 전부 통과했을 때만 배포한다.
- 성공적인 푸시는 Cloudflare Pages를 트리거한다. `https://j-live.kr/calendar/`와 변경된 모든 클린 이벤트 URL이 새 콘텐츠를 반환하는지 검증한다. 검증이나 푸시가 실패하면 명확히 보고하고 배포 성공을 주장하지 않는다. 아무것도 바뀌지 않았으면 커밋하거나 배포하지 않는다.

## 10. 보고 형식 (한국어)

다음을 간결한 한국어로 보고한다:

- 변경/신규 추가된 이벤트와 모든 미래 일반예매·선예매 날짜/시간/자격
- 변경된 이벤트별 좌석 등급과 가격, 정확한 공식 소스 링크
- 검색용으로 추가된 별칭
- 캐싱된 프로필 사진 변경사항
- 취소·연기
- 미해결(pending) 항목과 정확한 누락/상충 사실
- 미검증 가격 사실
- 사용된 모든 공식 소스 URL
- 실행한 테스트, 커밋 해시, 푸시 결과, 실서비스 배포 검증 결과

아무것도 바뀌지 않았으면 실제로 확인한 공식 티켓 사이트, 아티스트/기획사 페이지, 인스타그램 계정을 나열한다.
